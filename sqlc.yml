version: "2"
sql:
  - engine: "postgresql"
    queries: "db/sqlc"
    schema: "db/migrations"
    gen:
      go:
        package: "repository"
        out: "pkg/repository"
        sql_package: "pgx/v5"
